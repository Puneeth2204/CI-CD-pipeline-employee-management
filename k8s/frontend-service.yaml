apiVersion: v1
kind: Service
metadata:
  name: employee-frontend-service
spec:
  type: NodePort
  selector:
    app: employee-frontend
  ports:
    - protocol: TCP
      port: 5173
      targetPort: 5173
